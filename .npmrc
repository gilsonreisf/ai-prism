# The server is pre-bundled (server-dist/) and the client is pre-built
# (client/dist/), so the deployed app needs ZERO packages at install time.
# Skip devDependencies so the Databricks build never hits the npm proxy.
omit=dev
audit=false
fund=false
